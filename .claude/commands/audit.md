Your goal is to update any vulnerable dependencies.

Do the Following:

1. Run `pnpm audit` to find vulnerable installed packaes in this project
2. Run `pnpm audit fix` to apply updates. 
3. Run tests and verify the updates didn't break anything.